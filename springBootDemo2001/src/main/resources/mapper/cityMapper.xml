<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Configuration 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="com.thornBird.sbdt.modules.test.dao.CityDao">

	<!-- getCitiesByCountryId2(int countryId); -->
    <select id="getCitiesByCountryId2" parameterType="java.lang.Integer" resultType="City">
        select * from m_city where country_id = #{countryId}
    </select>
</mapper>